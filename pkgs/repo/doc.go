// Package repo provides safe path resolution under a configured repository root,
// file search for @-mention suggestions, line counting, and validation of
// @path and @path(start-end) tokens in prompts.
package repo
