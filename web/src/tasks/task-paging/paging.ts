/** Server page size for `GET /tasks` from the home list. */
export const TASK_LIST_PAGE_SIZE = 20;

/** Server page size for `GET /tasks/{id}/events` on the task detail timeline. */
export const TASK_EVENTS_PAGE_SIZE = 20;
